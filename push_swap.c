#include "push_swap.h"

char    **read_numbers(char **arr_nb, int size)
{
    char **res = (char **)malloc(size * sizeof(char *));
    int i = 1;
    int j = 0;
    while (i < size)
    {
        
    }
    
}


int main (int ac, char **av)
{
    if (ac < 2)
    {
        printf("error");
        exit(1);
    }
    
}